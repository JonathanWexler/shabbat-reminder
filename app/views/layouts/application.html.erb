<!DOCTYPE html>
<html>
<head>
  <title>ShabbatReminder</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => false %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="indigo accent-4">
    <div class="container">

        <div class="nav-wrapper ">
            <a href="#!" class="brand-logo">Shabbat Reminder</a>
          <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">|||</i></a>
          <ul class="right hide-on-med-and-down">
            <li><%= link_to(current_user.name, current_user) if current_user%></li>
            <li><%= link_to("Set Reminders", user_reminders_path(current_user)) if current_user%></li>
            <li><%= link_to("Settings", user_settings_path(current_user)) if current_user%></li>
            <li><%= link_to("Facebook Connect", current_user) if current_user%></li>
            <% if current_user%>
            <li><%= link_to "Sign out", signout_path %></li>
            <%else%>
            <li><%= link_to "Sign in With Facebook", 'auth/facebook' %></li>
            <% end%>
        </ul>
        <ul class="side-nav" id="mobile-demo">
            <li><%= link_to(current_user.name, current_user) if current_user%></li>
            <li><%= link_to("Set Reminders", user_reminders_path(current_user)) if current_user%></li>
            <li><%= link_to("Settings", user_settings_path(current_user)) if current_user%></li>
            <li><%= link_to("Facebook Connect", current_user) if current_user%></li>
            <% if current_user%>
            <li><%= link_to "Sign out", signout_path %></li>
            <%else%>
            <li><%= link_to "Sign in With Facebook", 'auth/facebook' %></li>
            <% end%>
        </ul>
    </div>
</div>
</nav>
<div class="container" id="main-container">
    <%= yield %>
</div>
<!-- o.H9zIXmg6pbY1Tz3UaYTRr0ytpdRSB15y -->
</body>
</html>
